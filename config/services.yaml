# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    env(DEFAULT_URI): 'https://localhost'
services:
    _defaults:
        autowire: true # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
    _instanceof:
        Symfony\Component\Console\Command\Command:
            tags: ["command"]

    Atoolo\CrawlerIndexer\:
        resource: "../src/"

    # Run in Development
    Atoolo\CrawlerIndexer\Command\Index:
        arguments:
            $indexerConfigurationLoader: '@atoolo_search.indexer.configuration_loader'

    # Run in Customer Projekt
    Atoolo\CrawlerIndexer\Application\Schedule:
        arguments:
            $schedule: "%atoolo.crawler.schedule%"

    Atoolo\CrawlerIndexer\Application\StartCrawlerMessageHandler:
        arguments:
            $indexerConfigurationLoader: '@atoolo_search.indexer.configuration_loader'

    Atoolo\CrawlerIndexer\Domain\Crawler\Ports\RequestExecutor:
        arguments:
            $retryStatusCodes: "%atoolo.crawler.retry_status_codes%"

    Atoolo\CrawlerIndexer\Controller\CrawlerManager:
        arguments:
            $urlCollector: '@Atoolo\CrawlerIndexer\Domain\Crawler\Steps\URLCollector'
            $fetcher: '@Atoolo\CrawlerIndexer\Domain\Crawler\Steps\Fetcher'
            $processor: '@Atoolo\CrawlerIndexer\Domain\Crawler\Steps\Processor'
            $logger: "@logger"
            $indexer: '@Atoolo\CrawlerIndexer\Domain\Crawler\Steps\Indexer'

    Atoolo\CrawlerIndexer\Domain\Crawler\Ports\RequestExecutorInterface: '@Atoolo\CrawlerIndexer\Domain\Crawler\Ports\RequestExecutor'

    # gehört eigentlich ins Kunden-FEDS bzw. Commons-FEDS
    Atoolo\CrawlerIndexer\Domain\Crawler\Steps\Indexer:
        arguments:
            $indexService: "@atoolo_search.indexer.solr_index_service"
            $progressHandler: "@atoolo_search.indexer.internal_resource_progress_state"

    Atoolo\CrawlerIndexer\Domain\Crawler\Services\URLNormalizer:
        arguments:
            $denyEndings: "%atoolo.crawler.deny_endings%"